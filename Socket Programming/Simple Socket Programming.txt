Function Define

1. Client
    - 사용자는 현재 소켓 버퍼 크기를 확인할 수 있다.
    - 사용자는 현재 소켓 버퍼 크기를 설정할 수 있다.

2. Server
    - Client로부터 받은 패킷 메시지를 분석해서 소켓 버퍼 크기 확인일 경우에는
      송신버퍼와 수신버퍼 크기를 확인해서 두 값을 Client로 전송
    - Client로부터 받은 패킷 메시지를 분석해서 소켓 버퍼 크기 설정일 경우에는
      같이 전달된 송수신 버퍼 크기값으로 소켓 송수신 버퍼 크기를 설정